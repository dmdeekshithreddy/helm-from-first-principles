apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "myfirst-helmchart.fullname" . }}-test-connection"
  labels:
    {{- include "myfirst-helmchart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "myfirst-helmchart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
