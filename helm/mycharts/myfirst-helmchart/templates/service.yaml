apiVersion: v1
kind: Service
metadata:
  name: {{ include "myfirst-helmchart.fullname" . }}
  labels:
    {{- include "myfirst-helmchart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "myfirst-helmchart.selectorLabels" . | nindent 4 }}
