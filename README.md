# helm-from-first-principles

Which ever is the current-context, Helm is connected to that cluster

```bash
kubectl config current-context
```

Add a new chart repository to install packages

```bash
helm repo add bitnami https://charts.bitnami.com/bitnami
```
